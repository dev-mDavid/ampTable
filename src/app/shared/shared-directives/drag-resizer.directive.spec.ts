import { DragResizerDirective } from './drag-resizer.directive';

describe('DragResizerDirective', () => {
  it('should create an instance', () => {
    const directive = new DragResizerDirective();
    expect(directive).toBeTruthy();
  });
});
